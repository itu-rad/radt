{
  "name": "radt-frontend-api-server",
  "version": "0.1.0",
  "description": "Lightweight server providing API endpoints for the frontend using Postgres",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
